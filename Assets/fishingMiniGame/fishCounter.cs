using UnityEngine;

public class fishCounter : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        



        // attach this to bucket.. +1 to counter when fish hits it.
        // when fish counter is => x then make the next fish caught the relic.
        // when counter has been added to then spawn splash effect



    }
}
